<p id="notice"><%= notice %></p>

<h1>Restaurants</h1>

<table>
  <thead>
    <tr>
      <th>Companynmae</th>
      <th>Description</th>
      <th>Cnpj</th>
      <th>Fantasyname</th>
      <th>City</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.companyNmae %></td>
        <td><%= restaurant.description %></td>
        <td><%= restaurant.cnpj %></td>
        <td><%= restaurant.fantasyName %></td>
        <td><%= restaurant.city_id %></td>
        <td><%= restaurant.category_id %></td>
        <td><%= link_to 'Photos', {controller: "photos", action: "index", restaurant_id: restaurant.id} %></td>
        <td><%= link_to 'Edit', edit_restaurant_path(restaurant) %></td>
        <td><%= link_to 'Destroy', restaurant, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Restaurant', new_restaurant_path %>
