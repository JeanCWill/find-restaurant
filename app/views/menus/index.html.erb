<p id="notice"><%= notice %></p>

<%= link_to 'Voltar', {controller: "restaurants", action: "index"}, class: "btn btn-default" %>

<h1>Produtos</h1>

<table class="table">
  <!--<thead>
    <tr>
      <th style="display: none">Restaurant</th>
      <th>Produto</th>
      <th>Preço</th>
      <th colspan="3"></th>
    </tr>
  </thead>-->

  <!--<tbody class="row">
    <% @menus.each do |menu| %>
      <tr class="col-md-3">
        <td style="display: none"><%= menu.restaurant_id %></td>
        <div class="row">
          <div class="col-lg-12">
            <td><%= link_to (image_tag menu.menu.thumb.url), menu, class:"btn btn-link" %></td>
          </div>
        </div>
        <div class="row">
          <div class="col-lg-12">
            <td><%= menu.price %></td>
            <td><%= link_to 'Excluir', menu, method: :delete, data: { confirm: 'Deseja mesmo excluir o produto?' }, class:"btn btn-link" %></td>
            </div>
        </div>
      </tr>
    <% end %>
  </tbody>-->
</table>

<div class="row">
  <% @menus.each do |menu| %>
    <div class="col-md-2">
      <div style="display: none"><%= menu.restaurant_id %></div>
      <div class="row">
        <div class="col-md-12">
          <div style="height: 200px;"><%= link_to (image_tag menu.menu.thumb.url), menu, class:"btn btn-link btn-img" %></div>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12">
          <%= link_to 'Excluir', menu, method: :delete, data: { confirm: 'Deseja mesmo excluir o produto?' }, class:"btn btn-link" %>
        </div>
      </div>
    </div>
  <% end %>
</div>

<br>

<%= link_to 'Adicionar produto', {controller: "menus", action: "new", restaurant_id: @restaurant_id }, class: "btn btn-primary" %>
